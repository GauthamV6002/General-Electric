#include "main.h"

namespace GE {
    class Flywheel { // Class Template for flywheel

        public: 
            enum FlywheelPreset {
                
            };

            // Main flywheel function deals with 
            void setFlywheel(FlywheelPreset preset, bool accelerate);
            void runFlywheel();



    };
}
